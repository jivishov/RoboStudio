.env
.env.*
!.env.example

node_modules/
dist/
coverage/
.playwright-mcp/

STL_files/
*.stl
*.STL
*.rar
artifacts/
backups/
# Where the Component Builder writes STEP/DXF exports. Build output, like STL_files/ - it
# was untracked once already (8a04c5a) and came straight back, because `git add -- .` keeps
# sweeping it in. Untracked without being ignored is a file somebody commits by accident.
design_files/

logs/
test-results/
*.png
*.mp4
!src/circuits/assets/photoreal/**/*.png

AGENTS.md
plan/
release_quality_plan_*/

.DS_Store
Thumbs.db
!/AGENTS.md
